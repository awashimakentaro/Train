# .gitignore
#
# 【責務】
# - Git 管理対象からビルド成果物や個人環境依存ファイル、秘匿情報を除外する。
# - Expo/React Native プロジェクト特有の生成物を網羅し、差分ノイズを抑える。
# - `.env` などの秘密情報を追跡させないことでセキュリティ事故を防ぐ。
# 【使用箇所】
# - Git による add/commit 時の自動除外判定。
# - CI でのチェックアウト時（不要ファイルが含まれないことを保証）。
# 【やらないこと】
# - ドキュメント化されたアセットや設計資料までは除外しない。
# - 除外方針の背景説明（docs/ を参照）。
# 【他ファイルとの関係】
# - `.env.example` とペアで運用し、実ファイル `.env` を無視する。
# - docs/workflow.md で述べた環境構築手順を補完する。

# === Dependencies ===
node_modules/

# === Expo / build artifacts ===
.expo/
dist/
web-build/
expo-env.d.ts

# === Native projects generated by EAS ===
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
/ios
/android

# === Metro cache ===
.metro-health-check*

# === Debug logs ===
npm-debug.*
yarn-debug.*
yarn-error.*

# === macOS ===
.DS_Store
*.pem

# === Environment files ===
.env
.env.*
!.env.example
.envrc
.env*.local

# === TypeScript ===
*.tsbuildinfo

# === Project specific ===
app-example
筋トレ管理アプリUIデザイン.zip

# === Tooling ===
.vscode
