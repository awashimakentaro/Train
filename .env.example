# .env.example
#
# 【責務】
# - Supabase など外部サービスの接続情報をひな形として示し、実環境 `.env` の作成を支援する。
# - Expo (RN) で参照される公開プレフィックス付き env を整理する。
# 【使用箇所】
# - 開発者が `cp .env.example .env` で複製し、値を埋めてローカル実行時に読み込む。
# - EAS Build / Expo Router が `process.env` 経由で参照する設定。
# 【やらないこと】
# - 実際のシークレット値を格納しない。
# - Supabase 以外の無関係サービスの設定は記述しない。
# 【他ファイルとの関係】
# - `.gitignore` が `.env` を除外するため、実ファイルは Git に載らない。
# - docs/requirements/supabase.md（作成予定）で詳細契約を説明する。

# コピー手順: `cp .env.example .env` を実行し、以下を実値で上書きしてください。
EXPO_PUBLIC_APP_ENV=development
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=replace-with-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
EXPO_PUBLIC_SUPABASE_STORAGE_BUCKET=training-assets
